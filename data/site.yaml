# Assemble directories & options
assemble:
    system:
        root: bonnet
        layouts: layouts
        helpers: lib/helpers
        partials: partials
    content:
        root: content
    options:
        layout: master

consoleColor:
    normal: cyan
    warning: yellow
    debug: blue
    error: red
